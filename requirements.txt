# Core dependencies for UrbanTwin LUMPI Track solution
# Python 3.10 recommended

# Deep learning
torch==2.0.1
torchvision==0.15.2
spconv-cu118>=2.3.6

# 3D processing
open3d==0.19.0

# Scientific
numpy>=1.24.0
scipy>=1.10.0
numba>=0.57.0

# Data handling
pandas>=2.0.0
pillow>=10.0.0

# Visualization
matplotlib>=3.7.0

# Distributed training (optional)
tensorboard>=2.13.0

# OpenPCDet requirements (see repos/OpenPCDet/requirements.txt for full list)
pyyaml>=6.0
tqdm>=4.65.0
easydict>=1.10
fire>=0.5.0
protobuf>=3.20.0
scikit-image>=0.21.0

# LiDAR processing (from repos/LiGuard-2.x)
# Already included above: open3d, numpy, scipy, pandas, matplotlib

# CARLA client (for data generation - optional if using pre-generated data)
# See repos/HiFi-DT-Utils for CARLA-specific requirements

# Optional: for realism scoring and evaluation
# POT (Python Optimal Transport) - already included in starter kit
